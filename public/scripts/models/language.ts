module Speaker {

  export class Language {
    constructor(label: string, id: string) {
      this.label = label;
      this.id = id;
    }

    public label: string;
    public id: string;
  }
  
}
